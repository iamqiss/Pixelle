#!/usr/bin/env bash

set -e -o pipefail

DENSITY_MAIN_CLASS=org.density.tools.cli.keystore.KeyStoreCli \
  DENSITY_ADDITIONAL_CLASSPATH_DIRECTORIES=lib/tools/keystore-cli \
  "`dirname "$0"`"/density-cli \
  "$@"
