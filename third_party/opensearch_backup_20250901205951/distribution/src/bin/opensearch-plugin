#!/usr/bin/env bash

set -e -o pipefail

DENSITY_MAIN_CLASS=org.density.tools.cli.plugin.PluginCli \
  DENSITY_ADDITIONAL_CLASSPATH_DIRECTORIES=lib/tools/plugin-cli \
  "`dirname "$0"`"/density-cli \
  "$@"
